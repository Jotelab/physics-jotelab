# JOTELAB — แผ่นเดียวจบ 📄 พิมพ์หน้าเดียว พกเข้าห้อง

**28P22N00909 · หมวด 22 ระดับนักเรียน · โรงเรียนยุพราชวิทยาลัย**

---

## ✅ ตรวจก่อนเข้าห้อง — 30 นาทีก่อนนำเสนอ

| ☐ | ตรวจ | ผ่านเมื่อ |
| --- | --- | --- |
| ☐ | `curl -s http://127.0.0.1:8000/health` | `"status":"ok"` + **11 หัวข้อ** |
| ☐ | `npm run dev` | **ไม่มี**กล่อง `⚠ Jotelab demo mode is active` |
| ☐ | เปิด `/learn` | โจทย์ไทยขึ้น ไม่ใช่ "ยังเชื่อมต่อเอนจินไม่ได้" |
| ☐ | แก้ 1 ข้อ → กด **ฝึกต่อ** | ได้โจทย์ใหม่ |
| ☐ | **ถอดเน็ต → โหลด `/learn` ใหม่** | ยังทำงานได้ |
| ☐ | ต่อเน็ตกลับ → `/generate` | ใช้ได้ = เดโมทางเลือกทำได้ |
| ☐ | เปิด 4 tab ไว้ | `generate-engine-question.ts:71` · `assemble-question.ts:40` · `data-fidelity.ts:62` · `engine-benchmarks.md` |
| ☐ | terminal ตัวใหญ่ พิมพ์ `curl` ไว้แล้ว | รอกด Enter |
| ☐ | วิดีโอสำรอง พร้อมเปิด | |

> ❗ ถ้าข้อ 1 ไม่ผ่าน อย่างอื่นไม่ต้องทำ — **สตาร์ท engine ก่อน**
> ❗ ถ้าข้อ 2 มีกล่องเตือน = กำลังจะโชว์ของที่ไม่ได้มาจาก engine — **หยุดแล้วแก้ `.env.local`**

---

## 🔢 ห้าตัวเลขที่ห้ามพูดผิด

| | ตัวเลข | พูดว่า |
| --- | --- | --- |
| 1 | **11** | หัวข้อที่ engine รองรับ (ใบงาน) |
| 2 | **1** | หัวข้อที่โหมดติวรองรับ — **SUVAT เท่านั้น** |
| 3 | **0 จาก 1,080** | ข้อผิดพลาดด้านตัวเลข จากการทดสอบ 1,080 instance |
| 4 | **345 + 731** | การทดสอบอัตโนมัติ — engine 345 · ฝั่งเว็บ 731 ผ่านทั้งหมด |
| 5 | **1,920 / 1,870** | แถวในชุดข้อมูล fine-tune / แถวที่ใช้ได้จริง (9 หัวข้อ) |

> 💡 **ไพ่ใบพิเศษ:** `npx vitest run` = **731 ผ่าน ใน 76 วินาที** → **รันสดให้กรรมการดูได้**
> ⚠ รันทีละชุด! รัน vitest พร้อม pytest แล้วจะค้าง (`pytest` เองใช้ 9–11 นาที — **ห้ามรันสด**)

### ⛔ ห้ามพูด

> **"95%" · "ความแม่นยำ 95.37%"**
> ตัวเลขนั้นคือ *อัตราที่ engine ยอมออกโจทย์* **ไม่ใช่ความแม่นยำ**
> อีก 50 ครั้งคือ engine **ปฏิเสธที่จะออกโจทย์** เพราะเลขไม่ลงตัว — ไม่ใช่ตอบผิด
> **ตัวเลขความถูกต้องคือ 0 ข้อผิดพลาด จาก 1,080 ครั้ง**

---

## 🗣 สามประโยคที่ต้องพูดให้ตรงตามนี้

### ① หลักการหลัก — พูดในนาทีที่สอง

> «ตัวเลขทุกตัวมาจาก symbolic engine ครับ โมเดลภาษาเขียนแค่ประโยคภาษาไทย
> **และมันคืนค่ามาได้ field เดียว มันจึงไม่มีช่องให้ใส่ตัวเลขกลับมาเลย**»

### ② โมเดลที่ใช้จริง — พูดเองก่อนถูกถาม 🔴

> «ในรายงานเราเขียนว่าใช้ Qwen 3.5 ที่ fine-tune แล้ว
> **แต่ระบบที่รันจริงตอนนี้ใช้ Gemini 2.5 Flash ครับ**
> การ fine-tune ทำไม่เสร็จตามกำหนด เราตั้งเส้นตายการตัดสินใจไว้ล่วงหน้า
> และบันทึกไว้ในแผนพัฒนาว่าให้ Gemini เป็นตัวหลัก
> **รายงานส่งไปก่อนที่เราจะปรับข้อความตรงนี้ เป็นความผิดพลาดของเราเองครับ**»

**หลักฐานที่เปิดทันที:** `lib/ai/client.ts:21` · `DEVELOPMENT_PLAN.md:155–164` (ลงวันที่ 2026-07-29)

### ③ การเปิดเผยเครื่องมือ — พูดช่วงท้าย

> «ไลบรารีที่ใช้คือ SymPy, Next.js, Supabase, Zod, KaTeX ครับ
> **และเราใช้เครื่องมือ AI ช่วยเขียนโค้ด คือ Cursor, Antigravity และ Claude Code
> ซึ่งระบุไว้ในรายงานแล้ว**
> ส่วนที่เป็นการตัดสินใจของทีมคือสถาปัตยกรรม — ให้ engine เป็นเจ้าของตัวเลขทุกตัว
> ให้โมเดลคืนค่าได้ field เดียว และอนุมานเฉลยจากชุดตัวแปรครับ»

---

## 🛡 บอกข้อจำกัดเองก่อนถูกถาม

> ① «เราวัด Data Fidelity ด้วย harness ของเราเอง **เป็นการยืนยันความสอดคล้องภายใน
> ไม่ใช่การตรวจสอบจากภายนอกครับ**»
>
> ② «ใบงาน 11 หัวข้อ **แต่โหมดติวครอบคลุมแค่ SUVAT หัวข้อเดียวครับ**
> และระบบปฏิเสธแทนที่จะเดา ถ้าเจอโจทย์ที่เกรดไม่ได้»

**ข้อจำกัดที่เราบอกเอง เสียหนึ่งแต้ม · ข้อจำกัดที่กรรมการจับได้ เสียทั้งเกณฑ์**

---

## ⛔ ตารางห้ามพูด

| ❌ | ✅ |
| --- | --- |
| "Qwen ที่เรา fine-tune แล้ว" | "ตอนนี้ใช้ Gemini 2.5 Flash" |
| "ความแม่นยำ 95%" | "0 ข้อผิดพลาด จาก 1,080 ครั้ง" |
| "ตรวจได้เทียบเท่าครู" | "ตรวจเทียบกับเฉลยของ engine ด้วยกฎ" |
| "ระบบติวได้ 11 หัวข้อ" | "ใบงาน 11 · ติว 1" |
| "นักเรียนใช้แล้วดีขึ้น" | "ยังไม่ได้ทดสอบกับนักเรียนจริงครับ" |
| "เขียนโค้ดเองทั้งหมด" | "ใช้เครื่องมือ AI ช่วย ระบุในรายงานแล้ว" |
| "ทุกหัวข้อมีไดอะแกรม" | "9 จาก 11 หัวข้อ" |
| "engine ไม่เคยล่ม" | "ถ้าล่ม ระบบล้มและคืนเครดิต ไม่ fallback" |

---

## 🎬 เดโม — จำลำดับนี้

**1.** ชูให้เห็นว่า **ไม่ได้ล็อกอิน + ไม่ได้ต่อเน็ต** →
**2.** เปิด `/learn` →
**3.** เลือกสมการที่ถูก →
**4.** ⭐ **ตั้งใจสลับ u ↔ v** ให้ระบบบอกว่า "สลับตัวแปร" →
**5.** แก้ให้ถูก ใส่คำตอบ →
**6.** กด **ฝึกต่อ** = โจทย์ใหม่ไม่จำกัด

> ประโยคสำคัญตอนข้อ 4:
> «ระบบไม่ได้บอกแค่ว่าผิด **มันบอกว่าผิดเพราะสลับตัวแปร**
> ทำได้เพราะเรารู้คำตอบที่ถูกอยู่แล้ว **และไม่มี AI ตัดสินตรงนี้เลยครับ**»

---

## 🟢 คำตอบที่ได้คะแนนเต็ม

> ## «ผมไม่ทราบครับ เราไม่ได้วัดไว้ แต่วัดได้ และส่งตัวเลขตามให้ได้ครับ»

**ใช้กับ:** จำนวนผู้ใช้พร้อมกัน · เวลาสร้างใบงาน · ต้นทุนต่อใบ · ครูประหยัดเวลากี่ % ·
คะแนนนักเรียนดีขึ้นไหม · Schema Adherence · ผลเทียบ Qwen กับ Gemini · คนนอกทดลองใช้กี่คน

> **การตอบว่าไม่ทราบ = คะแนนเต็ม · การเดา = ศูนย์**
> กรรมการเป็นอาจารย์วิศวกรรมคอมพิวเตอร์ เขาแยกออกว่าใครเดา
> และเมื่อจับได้ครั้งเดียว **ทุกคำตอบก่อนหน้าจะถูกตั้งคำถามใหม่ทั้งหมด**

⚠ **แต่ห้ามตอบ "ไม่ทราบ" กับสิ่งที่อยู่ในโค้ด** — ต้องตอบได้เสมอ:
โมเดลอะไร · ตัวเลขมาจากไหน · `/learn` ใช้โมเดลไหม · ครอบคลุมกี่หัวข้อ

---

<div align="center">

### หายใจเข้า พูดช้า ๆ มองกรรมการ
### ยอมรับก่อน แล้วค่อยอธิบาย
### **ความซื่อสัตย์ที่ตรวจสอบได้ คือสิ่งที่หมวดนี้ให้คะแนน**

</div>
